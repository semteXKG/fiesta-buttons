#pragma once

typedef enum {
    BUTTON_NAME_PIT = 1,
    BUTTON_NAME_YES  = 2,
    BUTTON_NAME_FCK = 3,
    BUTTON_NAME_STINT = 4,
    BUTTON_NAME_NO   = 5
} ButtonName;

typedef enum {
    BUTTON_STATE_PRESSED = 1,
    BUTTON_STATE_DEPRESSED = 2
} ButtonState;
